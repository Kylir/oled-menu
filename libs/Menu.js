/**
 * Parses a JSON menu and uses display.renderMenu() to display the visible items of the menu.
 * The program also uses a control object to receive user interaction.
 */

let Menu = (menus, display, control) => {
  let currentNode,
    parentNode
  
  

}

module.exports = Menu
